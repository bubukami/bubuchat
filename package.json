{
  "name": "bubuchat",
  "version": "1.0.0",
  "description": "BubuChat - 超轻量级实时聊天",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm install && npm start",
    "client": "cd client && npm install && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
